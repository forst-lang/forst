// Auto-generated client for user package
// Generated by Forst TypeScript Transformer

import { ForstSidecarClient } from "@forst/sidecar";
import * as types from "./types";

export const user = (client: ForstSidecarClient) => ({
  CreateUser: async (input: types.CreateUserRequest) => {
    const response = await client.invokeFunction("user", "CreateUser", input);
    if (!response.success) {
      throw new Error(response.error || "user.CreateUser failed");
    }
    return response.result;
  },
  GetUserById: async (id: string) => {
    const response = await client.invokeFunction("user", "GetUserById", { id });
    if (!response.success) {
      throw new Error(response.error || "user.GetUserById failed");
    }
    return response.result;
  },
  UpdateUserAge: async (id: string, newAge: number) => {
    const response = await client.invokeFunction("user", "UpdateUserAge", {
      id,
      newAge,
    });
    if (!response.success) {
      throw new Error(response.error || "user.UpdateUserAge failed");
    }
    return response.result;
  },
});
