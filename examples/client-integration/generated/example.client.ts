// Auto-generated client for user package
// Generated by Forst TypeScript Transformer

import { ForstClient as SidecarClient } from '@forst/sidecar';
import * as types from './types';

export const user = (client: SidecarClient) => ({
  CreateUser: async (input: CreateUserRequest) => {
    const response = await client.invokeFunction('user', 'CreateUser', input: CreateUserRequest);
    if (!response.success) {
      throw new Error(response.error || 'user.CreateUser failed');
    }
    return response.result;
  },
  GetUserById: async (id: any) => {
    const response = await client.invokeFunction('user', 'GetUserById', id: any);
    if (!response.success) {
      throw new Error(response.error || 'user.GetUserById failed');
    }
    return response.result;
  },
  UpdateUserAge: async (id: any, newAge: any) => {
    const response = await client.invokeFunction('user', 'UpdateUserAge', id: any, newAge: any);
    if (!response.success) {
      throw new Error(response.error || 'user.UpdateUserAge failed');
    }
    return response.result;
  },
});
