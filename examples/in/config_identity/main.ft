package main

import (
	"example.com/config_identity/app"
	"example.com/config_identity/config"
	"fmt"
)

func main() {
	cfg := config.Load()
	err := app.Run(cfg)
	ensure !err
	fmt.Println(cfg.Port)
}
