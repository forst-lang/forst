package main

// Field assignment on a value-shaped parameter must persist through `return a`.
// Golden output: ../out/shape_value_mutate.go
type Acc = {n: Int}

func bump(a Acc): Acc {
	a.n = a.n + 1
	return a
}

func main() {
	a := Acc{n: 0}
	a = bump(a)
	println(a.n)
}
