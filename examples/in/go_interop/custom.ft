package main

// Same-package Go named type Bag: construct, return, and field access from Forst.
func makeBag(): Bag {
	return Bag{Name: "forst"}
}

func bagLabel(b Bag): String {
	return b.Name
}

func runBagDemo() {
	b := makeBag()
	println(BagName(b))
	println(bagLabel(b))
}
