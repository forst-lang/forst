package main

import "os/exec"

// runExecDemo exercises Go FFI: field access, method calls, subslices, variadic spread.
func runExecDemo() {
	argv := []String{"true", "extra"}
	cmd := exec.Command(argv[0], argv[1:]...)
	cmd.Run()
	cmd.ProcessState.ExitCode()
}
