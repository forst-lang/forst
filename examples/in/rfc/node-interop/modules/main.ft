package main

import checkout "./legacy/api/checkout" node
import "strconv"

func main() {
	order := checkout.createOrder(100.0, "USD")
	ensure order is Ok()
	println(order.id)
	println(order.total)
	println(strconv.FormatFloat(order.totalCents, 'f', 0, 64))
}
