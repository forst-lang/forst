package main

import node "./legacy/payment"

func main() {
	result := payment.create(100.0, "USD")
	ensure result is Ok()
	println(result.id)

	echo := payment.concurrentEcho(7.0)
	ensure echo is Ok()
	println(echo.echo)
}
