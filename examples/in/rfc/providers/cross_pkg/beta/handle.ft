// Cross-package Providers cookbook: beta calls alpha; Logger propagates to Handle.
package beta

import "providers_cross_pkg_demo/alpha"

type Logger = {
	Info(msg String),
}

func Handle(id String) {
	alpha.LogExpiry(id)
}
