// Cross-package Providers cookbook: auth defines Logger contract + direct use.
package auth

type Logger = {Info(msg String)}

type NopLogger = {}

func (NopLogger) Info(msg String) {
}

func LogEvent(id String) {
	use logger: Logger
	logger.Info("expire " + id)
}
