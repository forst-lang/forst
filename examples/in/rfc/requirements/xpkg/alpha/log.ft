// Cross-package Usables cookbook: alpha defines Logger contract + direct use.
package alpha

type Logger = {
	info(msg String),
}

func LogExpiry(id String) {
	use logger: Logger
	logger.info("expire " + id)
}
