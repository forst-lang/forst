// Cross-package Usables cookbook: beta calls alpha; Logger propagates to Handle.
package beta

import "usables_xpkg_demo/alpha"

type Logger = {
	info(msg String)
}

func Handle(id String) {
	alpha.LogExpiry(id)
}
