package main

import "./legacy/payment" js

func main() {
	// Phase 1: compile-time Node interop — sync call into legacy/payment.ts
	result := payment.create(100.0, "USD")
	ensure result is Ok()
	println(result.id)
}
