package main

type User = {
    name: String
    age: Int
    role: String
}

is (user User) Adult {
    ensure user.age is Min(18)
}

func acceptAdult(user User) {}

func check(user User) {
    ensure user is Adult()
    p := &user
    p.age = 12
    acceptAdult(user)
}

func main() {}
