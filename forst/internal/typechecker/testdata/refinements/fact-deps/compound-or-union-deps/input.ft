package main

type User = {
	name: String
	age: Int
	role: String
}

is (user User) Adult {
	ensure user.age is Min(18)
}

is (user User) Admin {
	ensure user.role is Min(1)
}

func check(user User) {
	ensure user is Adult() or Admin()
}

func main() {}
