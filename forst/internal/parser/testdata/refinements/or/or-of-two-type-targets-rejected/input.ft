package main

type ActiveStatus = "pending" | "processing"
type FailedStatus = "failed"

func check(status String) {
	ensure status is ActiveStatus or FailedStatus else {
		return
	}
}

func main() {}
