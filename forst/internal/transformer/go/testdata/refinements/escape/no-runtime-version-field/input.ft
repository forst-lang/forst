package main

type User = {
    name: String
    age: Int
    role: String
}

is (user User) Adult {
    ensure user.age is Min(18)
}

func acceptAdult(user User) {}

func check(user User) {
    ensure user is Adult()
    user.name = "Alice"
    acceptAdult(user)
}

func main() {}
