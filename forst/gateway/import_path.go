package gateway

// StdlibImportPath is the Go import path for this merge-path stdlib package (executor temp modules,
// typechecker go/packages loading, etc.).
const StdlibImportPath = "forst/gateway"
