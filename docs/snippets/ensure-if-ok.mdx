<Tabs>
  <Tab title="Forst">
    ```ft
    if x is Ok() {
    	println(x) // x narrowed inside this branch
    }
    ```
  </Tab>
  <Tab title="Generated Go">
    ```go
    // forst build — Result lowers to (T, error)
    x, xErr := one()
    if xErr == nil {
    	println(x)
    }
    ```
  </Tab>
</Tabs>
