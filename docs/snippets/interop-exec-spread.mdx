<Tabs>
  <Tab title="Forst">
    ```ft
    package main

    import "os/exec"

    func main() {
    	argv := []String{"true", "extra"}
    	cmd := exec.Command(argv[0], argv[1:]...)
    	cmd.Run()
    	cmd.ProcessState.ExitCode()
    }
    ```
  </Tab>
  <Tab title="Generated Go">
    ```go
    // forst build
    package main

    import "os/exec"

    func main() {
    	argv := []string{"true", "extra"}
    	cmd := exec.Command(argv[0], argv[1:]...)
    	cmd.Run()
    	cmd.ProcessState.ExitCode()
    }
    ```
  </Tab>
</Tabs>
